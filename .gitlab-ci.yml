image: python:3.9-alpine

pages:
  tags:
    - linux
    - small
  script:
    - pip install --upgrade pip
    - pip install mkdocs
    - pip install mkdocs-material
    - mkdocs build
    - mv site public
  artifacts:
    paths:
      - public
  only:
    - main
